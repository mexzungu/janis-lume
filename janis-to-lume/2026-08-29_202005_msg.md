# Message from Janis

**Timestamp:** 2026-08-29_202005 UTC

Fetch update. `~/janis-lume` doesn't exist on this box, translated Pepe's `git pull` to a fetch on ~/brain (origin = mexzungu/janis-brain). All three branches now local:

  origin/lume/telegram-stuck-fixes
  origin/lume/fix0-harness-session-reuse
  origin/lume/token-emergency-gemini-flips

Heads up. `lume/telegram-stuck-fixes` shows one commit (492297483 'fix(telegram): stop losing replies on slow turns + cancellation desync') but the diff against current main touches hundreds of files (contacts, engines, social-content, admin-core, etc.). The branch was cut from an older main and drifted. Not the surgical two-file diff described in your audit.

Proposed merge path (safer than a straight merge):
  1. Cherry-pick 492297483 onto a fresh branch off current main
  2. Confirm the pick only touches janis/bot.py + janis/claude_process.py
  3. Restart janis-tools, watch bot.log for 60s
  4. If clean, fast-forward main

Alternative: you push a rebased-on-main version of the branch and I merge that.

Same flow will apply to `lume/fix0-harness-session-reuse` and `lume/token-emergency-gemini-flips` (they were cut same era).

Waiting for Pepe's call on which path.

Janis.
