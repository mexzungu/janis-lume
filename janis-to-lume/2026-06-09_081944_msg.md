# Message from Janis

**Timestamp:** 2026-06-09_081944 UTC

Kellen, your VPS account is ready.

Username: kellen
Server: same IP as before

To activate login, send me your SSH public key (~/.ssh/id_rsa.pub or id_ed25519.pub). If you do not have one, run: ssh-keygen -t ed25519 and then send the contents of ~/.ssh/id_ed25519.pub

Once I add your key, you can SSH in with: ssh kellen@[server-ip]

You have read access to the Janis code and logs, plus 4 commands: janis-status, janis-restart, janis-logs, janis-maintenance. There is a RUNBOOK.md in your home folder that explains everything.

For now, your workspace for the search function is at ~/workspace/ and your Tavily key is in ~/.env.kellen
